# SmartCode 端到端评测报告（简化版）
> 生成时间: 2026-07-08 16:21:35
> 评测模型: qwen-max
> 任务数: 10（单轮单文件）

## 总体结果

| 模式 | 正确数 | 总数 | 正确率 | 总耗时 |
|:-----|:------:|:----:|:------:|:------:|
| off（无审查） | 10 | 10 | 100.0% | 141s |
| strict（审查） | 10 | 10 | 100.0% | 162s |

## 逐任务详情

### S01 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 9.5s
- **评语**: 代码完全符合需求，正确实现了判断偶数的功能，并且包含了类型检查。
- **代码**:
  ```python
# === even.py ===
"""Module providing is_even function."""


def is_even(n: int) -> bool:
    """Determine if an integer is even.

    Args:
        n: Integer to check.

    Returns:
        True if n is even, False otherwise.

    Raises:
        TypeError: If n is not an integer.
    """
    if not isinstance(n, int):
        raise TypeError(f"Expected int, got {type(n).__name__}")
    return n % 2 == 0


  ```
### S01 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 12.7s
- **评语**: 代码完全符合需求，正确实现了判断整数是否为偶数的功能。
- **代码**:
  ```python
# === even.py ===
def is_even(n: int) -> bool:
    """判断整数是否为偶数。

    Args:
        n: 要判断的整数。

    Returns:
        如果 n 是偶数返回 True，否则返回 False。
    """
    return n % 2 == 0


  ```
### S02 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 20.2s
- **评语**: 代码正确实现了字符串反转功能。
- **代码**:
  ```python
# === reverse.py ===
def reverse_string(s: str) -> str:
    """反转字符串。

    Args:
        s: 要反转的字符串。

    Returns:
        反转后的字符串。

    Examples:
        >>> reverse_string("hello")
        'olleh'
        >>> reverse_string("")
        ''
        >>> reverse_string("a")
        'a'
        >>> reverse_string("Python")
        'nohtyP'
    """
    return s[::-1]


  ```
### S02 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 16.2s
- **评语**: 代码正确实现了字符串反转功能。
- **代码**:
  ```python
# === reverse.py ===
def reverse_string(s: str) -> str:
    """Reverse the input string and return it."""
    return s[::-1]


  ```
### S03 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 12.8s
- **评语**: 代码完全符合需求，正确实现了阶乘计算功能，并包含了适当的错误处理。
- **代码**:
  ```python
# === fact.py ===
def factorial(n: int):
    """Calculate the factorial of a non-negative integer n.

    Factorial of n (n!) is the product of all positive integers less than or equal to n.
    By convention, factorial(0) = 1.

    Args:
        n: A non-negative integer.

    Returns:
        The factorial of n as an integer.

    Raises:
        ValueError: If n is negative.
        TypeError: If n is not an integer.
    """
    if not isinstance(n, int):
        raise TypeError("n must be an integer")
    if n < 0:
        raise ValueError("n must be non-negative")

    if n == 0:
        return 1

    result = 1
    for i in range(1, n + 1):
        result *= i
    return result


  ```
### S03 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 15.7s
- **评语**: 代码完全符合需求，正确实现了阶乘计算功能。
- **代码**:
  ```python
# === fact.py ===
def factorial(n):
    """计算 n 的阶乘 (n!)."""
    if not isinstance(n, int):
        raise TypeError("factorial() expects an integer argument")
    if n < 0:
        raise ValueError("factorial() not defined for negative values")
    if n == 0:
        return 1
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result


  ```
### S04 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 14.1s
- **评语**: 代码正确实现了判断素数的功能，并且处理了非整数输入的情况。
- **代码**:
  ```python
# === prime.py ===
def is_prime(n):
    """Determine whether an integer n is a prime number.

    Args:
        n: The integer to check.

    Returns:
        True if n is a prime number, False otherwise.

    Raises:
        TypeError: If n is not an integer.
    """
    if not isinstance(n, int):
        raise TypeError("n must be an integer")

    if n <= 1:
        return False

    if n == 2:
        return True

    if n % 2 == 0:
        return False

    i = 3
    while i * i <= n:
        if n % i == 0:
            return False
        i += 2

    return True


  ```
### S04 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 17.7s
- **评语**: 代码正确实现了判断素数的功能，并且包含了对输入类型的检查。
- **代码**:
  ```python
# === prime.py ===
"""Prime number detection module."""


def is_prime(n):
    """Determine if a number is prime.

    Args:
        n: An integer to check.

    Returns:
        True if n is prime, False otherwise.

    Raises:
        TypeError: If n is not an integer.
    """
    if not isinstance(n, int):
        raise TypeError("Input must be an integer")

    if n < 2:
        return False

    if n == 2:
        return True

    if n % 2 == 0:
        return False

    i = 3
    while i * i <= n:
        if n % i == 0:
            return False
        i += 2

    return True


  ```
### S05 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 15.8s
- **评语**: 代码正确实现了斐波那契数列的计算，并且包含了对输入参数的有效性检查。
- **代码**:
  ```python
# === fib.py ===
def fibonacci(n):
    """返回第 n 个斐波那契数。

    Args:
        n: 非负整数，指定斐波那契数的索引。

    Returns:
        第 n 个斐波那契数。

    Raises:
        ValueError: 如果 n 不是整数或为负数。
    """
    if not isinstance(n, int):
        raise ValueError("n 必须是整数")
    if n < 0:
        raise ValueError("n 不能为负数")

    if n <= 1:
        return n

    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b
    return b


  ```
### S05 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 24.8s
- **评语**: 代码完全符合需求，正确实现了斐波那契数列的计算。
- **代码**:
  ```python
# === fib.py ===
def fibonacci(n: int) -> int:
    """Return the n-th Fibonacci number.

    The Fibonacci sequence is defined as:
        F(0) = 0, F(1) = 1
        F(n) = F(n-1) + F(n-2) for n >= 2

    Args:
        n: The index of the Fibonacci number to compute (non-negative integer).

    Returns:
        The n-th Fibonacci number.

    Raises:
        ValueError: If n is negative.
        ValueError: If n is not an integer.
    """
    if not isinstance(n, int):
        raise ValueError("n must be an integer")
    if n < 0:
        raise ValueError("n must be non-negative")

    if n == 0:
        return 0
    if n == 1:
        return 1

    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b
    return b


  ```
### S06 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 12.5s
- **评语**: 代码正确实现了统计字符串中元音字母数量的功能，包括大小写不敏感。
- **代码**:
  ```python
# === vowels.py ===
def count_vowels(s: str) -> int:
    """统计字符串中元音字母（a, e, i, o, u）的个数，不区分大小写。"""
    vowels = set("aeiouAEIOU")
    return sum(1 for ch in s if ch in vowels)


  ```
### S06 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 17.0s
- **评语**: 代码正确实现了统计字符串中元音字母数量的功能，且考虑了大小写。
- **代码**:
  ```python
# === vowels.py ===
def count_vowels(s: str) -> int:
    """Return the number of vowels (a, e, i, o, u) in the given string.

    The count is case-insensitive.
    """
    vowels = set("aeiouAEIOU")
    return sum(1 for ch in s if ch in vowels)


  ```
### S07 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 8.8s
- **评语**: 代码正确实现了计算列表元素之和的功能。
- **代码**:
  ```python
# === sum.py ===
def array_sum(arr):
    """返回列表中所有元素之和。"""
    return sum(arr)


  ```
### S07 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 13.7s
- **评语**: 代码正确实现了计算列表元素之和的功能。
- **代码**:
  ```python
# === sum.py ===
def array_sum(arr):
    """Return the sum of all elements in a list."""
    return sum(arr)


  ```
### S08 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 14.7s
- **评语**: 代码完全符合需求，实现了正确的FizzBuzz逻辑，并包含了适当的错误处理。
- **代码**:
  ```python
# === fizz.py ===
def fizzbuzz(n: int):
    """Return a FizzBuzz list from 1 to n.

    - Multiples of 3 -> "Fizz"
    - Multiples of 5 -> "Buzz"
    - Multiples of both 3 and 5 -> "FizzBuzz"
    - Otherwise -> the number itself

    Args:
        n: The upper bound (inclusive).

    Returns:
        A list of strings and integers representing the FizzBuzz sequence.

    Raises:
        ValueError: If n is less than 1.
        TypeError: If n is not an integer.
    """
    if not isinstance(n, int):
        raise TypeError("n must be an integer")
    if n < 1:
        raise ValueError("n must be at least 1")

    result = []
    for i in range(1, n + 1):
        if i % 15 == 0:
            result.append("FizzBuzz")
        elif i % 3 == 0:
            result.append("Fizz")
        elif i % 5 == 0:
            result.append("Buzz")
        else:
            result.append(i)
    return result


  ```
### S08 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 12.8s
- **评语**: 代码完全符合需求，正确实现了FizzBuzz逻辑。
- **代码**:
  ```python
# === fizz.py ===
def fizzbuzz(n):
    """Return a FizzBuzz list from 1 to n.

    - Multiples of 3 are replaced with "Fizz"
    - Multiples of 5 are replaced with "Buzz"
    - Multiples of both 3 and 5 are replaced with "FizzBuzz"

    Args:
        n: A positive integer.

    Returns:
        A list of length n, where each element is either an integer or a string.

    Raises:
        ValueError: If n is 0 or negative.
        TypeError: If n is not an integer.
    """
    if not isinstance(n, int):
        raise TypeError("n must be an integer")
    if n <= 0:
        raise ValueError("n must be a positive integer")

    result = []
    for i in range(1, n + 1):
        if i % 3 == 0 and i % 5 == 0:
            result.append("FizzBuzz")
        elif i % 3 == 0:
            result.append("Fizz")
        elif i % 5 == 0:
            result.append("Buzz")
        else:
            result.append(i)
    return result


  ```
### S09 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 13.4s
- **评语**: 代码正确实现了从摄氏度到华氏度的转换功能。
- **代码**:
  ```python
# === temp.py ===
"""Temperature conversion utilities."""


def celsius_to_fahrenheit(c: float) -> float:
    """Convert Celsius to Fahrenheit.

    Args:
        c: Temperature in Celsius.

    Returns:
        Temperature in Fahrenheit.
    """
    return round(c * 9.0 / 5.0 + 32.0, 1)


  ```
### S09 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 14.7s
- **评语**: 代码正确实现了从摄氏度到华氏度的转换。
- **代码**:
  ```python
# === temp.py ===
def celsius_to_fahrenheit(c):
    """Convert Celsius to Fahrenheit."""
    return c * 9.0 / 5.0 + 32.0


  ```
### S10 (off)
- **正确**: [PASS] (分数: 100)
- **耗时**: 19.2s
- **评语**: 代码正确实现了需求，能够判断一个字符串是否为回文，并且忽略了大小写和空格。
- **代码**:
  ```python
# === palin.py ===
def is_palindrome(s: str) -> bool:
    """Check if a string is a palindrome.

    Ignores case and spaces. Returns True for empty strings
    and single characters.
    """
    # Normalize: lowercase and remove spaces
    cleaned = s.lower().replace(" ", "")
    return cleaned == cleaned[::-1]


  ```
### S10 (strict)
- **正确**: [PASS] (分数: 100)
- **耗时**: 16.4s
- **评语**: 代码正确实现了需求，能够判断一个字符串是否为回文（不区分大小写和空格）。
- **代码**:
  ```python
# === palin.py ===
def is_palindrome(s: str) -> bool:
    """Check if a string is a palindrome (case-insensitive, space-insensitive).

    Args:
        s: The string to check.

    Returns:
        True if s is a palindrome, False otherwise.
    """
    # Remove all whitespace and convert to lowercase
    cleaned = "".join(s.split()).lower()
    return cleaned == cleaned[::-1]


  ```
