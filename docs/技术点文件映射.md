# 技术点 → 文件映射表

> 把你的项目概述逐条拆解，对应到需要读的文件和方法

---

## 一、Agent 循环引擎

### "搭建 think/act/verify 核心循环引擎"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| `agent_loop_lite.py` | `run_agent_turn()` — 主循环，Step A→B→C→D | L688 |
| `agent_loop_lite.py` | `_model_next()` — 调模型入口 | L661 |
| `turn_kernel.py` | `derive_turn_step_policy()` — Step A 策略推导 | L683 |
| `turn_kernel.py` | `decide_assistant_turn()` — Step C 决策（4 种返回） | L1365 |
| `turn_kernel.py` | `decide_tool_turn()` — Step D 工具决策 | L1588 |

### "分离决策、执行两大模块"

| 文件 | 角色 |
|------|------|
| **`agent_loop_lite.py`** | **执行引擎** — 调模型、执行工具、管理循环 |
| **`turn_kernel.py`** | **决策引擎** — 策略推导、4 种返回类型判断 |

### "兼容重试、降级、终止等多种模型返回分支"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| `turn_kernel.py` | `decide_assistant_turn()` 中的 6 步决策树 | L1365 |
| `turn_kernel.py` | `_is_recoverable_thinking_stop` 判断 | L894（lite） |
| `agent_loop_lite.py` | Step B 的 3 层异常处理（ConnectionError/TimeoutError/Exception） | L894-L942 |
| `agent_loop_lite.py` | `_should_attempt_model_fallback()` — 是否切换模型 | L157 |
| `agent_loop_lite.py` | `_summarize_model_api_failure()` — 错误消息翻译 | L198 |

### "通过状态管理器区分执行模式，解决多线程资源竞争问题"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| **`state.py`** | `Store` + `AppState` — 全局状态管理器 | L22-L158 |
| `agent_loop_lite.py` | 串行传 store、并行传 None 的区分逻辑 | L477/L666-L668 |
| `agent_loop_lite.py` | `_execute_single_tool()` 中的 `if store:` 分支 | L474-L507 |

---

## 二、工具系统

### "标准化工具注册执行体系"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| **`tooling.py`** | `ToolRegistry` — 注册/查找/执行总入口 | L368 |
| **`tooling.py`** | `ToolDefinition` — 工具定义（name/description/schema/validator/run） | L305 |
| **`tooling.py`** | `ToolResult` — 统一返回类型 | L260 |
| **`tooling.py`** | `ToolContext` — 执行上下文 | L285 |
| `tools/__init__.py` | `create_default_tool_registry()` — 注册所有工具 | L163 |
| `tools/read_file.py` | 一个完整工具的范例（_validate + _run + ToolDefinition） | 全文件 |

### "基于线程池增加超时熔断机制，避免工具死循环阻塞进程"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| `agent_loop_lite.py` | `_execute_single_tool()` 中的 ThreadPoolExecutor + timeout | L487-L493 |
| `agent_loop_lite.py` | 超时返回 ToolResult(ok=False) | L494-L495 |
| `agent_loop_lite.py` | 非超时异常同步重试兜底 | L496-L500 |

### "自动决定不同工具串行与并行执行，提升资源利用率"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| **`agent_intelligence.py`** | `ToolScheduler.schedule_calls()` — 并发/串行分类逻辑 | L450-L497 |
| `agent_loop_lite.py` | Step D 中的并行执行（ThreadPoolExecutor） | L1101-L1121 |
| `agent_loop_lite.py` | Step D 中的串行执行（for 循环） | L1123-L1136 |

---

## 三、会话与持久化

### "快照回退与增量持久化方案"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| **`session.py`** | `SessionData` — 完整会话数据结构 | L78 |
| **`session.py`** | `FileCheckpoint` — 文件修改前快照 | L65 |
| **`session.py`** | `create_file_checkpoint()` — 创建检查点 | L993 |
| **`session.py`** | `rewind_session_data()` — 回退执行 | L1103 |

### "支持步数、检查点两种回滚方式"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| `session.py` | `_select_checkpoints_to_rewind()` — 两种回退路径（steps / checkpoint_id） | L1063 |
| `session.py` | 按 steps 回退：`start_index = max(len - steps, 0)` | L1087 |
| `session.py` | 按 checkpoint_id 回退：从后向前查找匹配 ID | L1075-L1083 |
| `session.py` | group_id 分组：同组回退保持原子性 | L1079-L1082、L1088-L1093 |

### "回退时生成反向快照保障安全"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| `session.py` | `rewind_session_data()` 第 3 步 — 创建反向安全快照 | L1170-L1191 |
| `session.py` | 反向快照 kind="rewind" | L1187 |
| `session.py` | 逆序遍历选中检查点，跳过已处理文件（去重） | L1174-L1176 |

### "增量存储降低 IO 写入压力，定期合并数据保证一致性"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| `session.py` | `_save_delta()` — 增量保存（仅追加新增消息/转录/检查点） | L520 |
| `session.py` | `_consolidate_deltas()` — 合并清理差量文件 | L569 |
| `session.py` | `save_session()` — 全量 vs 增量决策（4 个条件） | L602-L729 |
| `session.py` | `load_session()` — 加载时合并 delta 文件 | L732-L883 |

---

## 四、上下文压缩

### "多级渐进式上下文压缩策略"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| **`context_manager.py`** | `compact_messages()` — 多级压缩执行 | L831 |
| `context_manager.py` | 第 0 级：删 assistant_progress | L868-L871 |
| `context_manager.py` | 第 1 级：截断大型工具结果（工具类型感知） | L889-L940 |
| `context_manager.py` | 第 2 级：压缩 tool_call+tool_result 对 | L948-L980 |
| `context_manager.py` | `_compress_tool_pair()` — 工具类型感知压缩 | L1026 |
| `context_manager.py` | `should_auto_compact()` — 多级触发阈值（95%→85%→75%→60%） | L811 |

### "熔断保护"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| **`circuit_breaker.py`** | `CompactionCircuitBreaker` — 熔断器实现 | L44 |
| `circuit_breaker.py` | 连续失败 N 次后跳闸 | L72-L74 |
| `agent_loop_lite.py` | `_try_compact_with_breaker()` — 熔断器包装压缩 | L639 |

---

## 五、记忆系统

### "搭建三层记忆系统"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| **`memory.py`** | `MemoryManager` — 三层记忆管理器入口 | L253 |
| **`memory.py`** | `MemoryEntry` — 记忆条目 | L83 |
| **`memory.py`** | `MemoryScope` — 三层作用域（user/project/local） | L50 |
| `memory_pipeline.py` | 记忆流水线编排 | 全文件 |
| `memory_reranker.py` | LLM 重排（top-15 → top-3） | 全文件 |

### "结合 BM25 检索实现历史会话知识复用"

| 文件 | 核心内容 | 行号 |
|------|---------|:----:|
| `memory.py` | `get_relevant_context()` — 检索入口 | L355（附近） |
| `memory.py` | `_score_entry()` — BM25 相关性评分 | 搜索 |
| `memory.py` | 检索流程：scope 排序 → BM25 评分 → 相关性过滤 → 合并 → token 截断 | L350-L400 |

---

## 六、必须读的文件（按优先级）

| 优先级 | 文件 | 对应技术点 | 估计时间 |
|:-----:|------|-----------|:-------:|
| 🔴 1 | `agent_loop_lite.py` | think/act/verify 循环、工具执行、状态管理 | 3h |
| 🔴 2 | `turn_kernel.py` | 4 种决策、策略推导、widen | 2h |
| 🔴 3 | `tooling.py` | 工具注册/执行体系 | 1h |
| 🔴 4 | `session.py` | checkpoint/rewind、增量持久化 | 2h |
| 🔴 5 | `memory.py` | 三层记忆、BM25 检索 | 1.5h |
| 🔴 6 | `context_manager.py` | 上下文压缩 | 1h |
| 🟡 7 | `agent_intelligence.py` | ToolScheduler 并发调度 | 0.5h |
| 🟡 8 | `state.py` | Store + AppState | 0.5h |
| 🟡 9 | `circuit_breaker.py` | 熔断保护 | 0.3h |
| 🔵 10 | `memory_pipeline.py` / `memory_reranker.py` | 记忆流水线 | 按需 |
