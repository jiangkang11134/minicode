# SmartCode 项目改动清单

> 完整记录本次改造中对各文件的所有修改，包含行号、修改内容和原因。
> 文件按"核心改造 → 审查系统 → 工具 → 配置文件"分组。

---

## 一、核心架构拆分

### 1. `agent_loop_lite.py` — **删除**

原文件 1225 行，核心逻辑拆分为 3 个文件后删除。外部引用全部改为 `agent_loop.py`。

### 2. `agent_loop.py` — **重写**

| 行号 | 修改 |
|:----:|:-----|
| L1-11 | **整文件重写**。原为 `from minicode.agent_loop_lite import run_agent_turn`，改为 `from minicode.loop_orchestrator import run_agent_turn`，直连编排引擎，移除中间层 |

### 3. `loop_orchestrator.py` — **新建（从 agent_loop_lite.py 拆分）**

`minicode/loop_orchestrator.py`，878 行。从原 `agent_loop_lite.py` 拆分出的主编排模块。

| 行号 | 修改 |
|:----:|:-----|
| L48 | `from minicode.tool_executor import _execute_single_tool, _register_tool_capabilities, init_review_hooks, _review_hooks` — **新增导入**：引入审查系统钩子初始化和工具执行模块 |
| L389 | `init_review_hooks(cwd, tools, ToolContext(...))` — **Prelude 阶段新增**：延迟初始化审查系统，传入 cwd/tools/context 三参数 |
| L853-855 | `if _review_hooks: _review_hooks.on_turn_end(current_messages)` — **Coda 阶段新增钩子 3**：注入审查发现到消息列表 + 沉淀重要发现 |

### 4. `model_caller.py` — **新建（从 agent_loop_lite.py 拆分）**

`minicode/model_caller.py`，280 行。封装 LLM 适配器调用、API 错误分类、回退判断、上下文阻塞限制、熔断器压缩。

| 行号 | 内容 |
|:----:|:-----|
| L1-280 | 从原 `agent_loop_lite.py` 拆分，包含 `_model_next()`、`_should_attempt_model_fallback()`、`_summarize_model_api_failure()`、`_is_at_blocking_limit()`、`_try_compact_with_breaker()` 等函数 |

### 5. `tool_executor.py` — **新建（从 agent_loop_lite.py 拆分）** + **钩子 1/2**

`minicode/tool_executor.py`，193 行。

| 行号 | 修改 |
|:----:|:-----|
| L22-27 | **新增**：`try/except ImportError` 导入审查系统钩子 `get_review_hooks`，`_review_hooks = None` 兜底。review/ 目录不存在时静默跳过 |
| L30-37 | **新增**：`init_review_hooks()` 函数，延迟初始化审查钩子。接收 `cwd/tools/tool_context` 三参数，在 `run_agent_turn()` 的 Prelude 阶段调用 |
| L123-129 | **新增钩子 1（写前宽松审查）**：`if _review_hooks and tool_name in ("write_file", "edit_file", "patch_file"):` → 调用 `_review_hooks.on_before_write()`，返回非 None 则阻断写入 |
| L167-173 | **新增钩子 2（写后处理）**：`if _review_hooks and result.ok and tool_name in ("write_file", "edit_file", "patch_file"):` → 从 `tool_input` 提取 `file_path`，调用 `_review_hooks.on_file_written()` 触发后台建表 + 严格审查 |

---

## 二、审查系统（新建 + 修改）

### 6. `review/config.py` — **修改**

| 行号 | 修改 |
|:----:|:-----|
| L1-11 | **文档更新**：新增运行时切换说明（off→strict 触发回补扫描，切换立即生效） |
| L17 | `REVIEW_MODE` 保留为向后兼容常量（模块导入时快照的值） |
| L28-34 | **新增 `get_review_mode()` 函数**：每次调用读 `os.environ.get()`，支持运行时模式切换，不依赖模块导入时的快照 |

### 7. `review/hooks.py` — **大幅修改 + 新增功能**

| 行号 | 修改 |
|:----:|:-----|
| L16-21 | **导入修改**：`REVIEW_MODE` 替换为 `get_review_mode`，支持运行时动态读取 |
| L26 | **新增**：`_fp_cache_lock = threading.Lock()` — 线程锁保护缓存的并发访问（修复 Defect 7） |
| L29-46 | **`get_review_hooks()` 重写**：不再 `REVIEW_MODE == "off"` 时返回 None，始终返回 ReviewHooks 实例。off 模式下钩子是 no-op，但实例活着才能检测 off→strict 过渡 |
| L54-59 | **类文档更新**：说明模式切换支持原理 |
| L61-71 | **`__init__` 重写**：新增 `_prev_mode`（模式切换检测状态）、`_retro_scan_done`（回补扫描只做一次标记） |
| L77-111 | **新增 `_check_transition()` 方法**：每个钩子入口调用。检测 off→loose/strict 过渡时触发全面回补扫描 |
| L113-119 | **新增 `_init_retroactive_scan()` 方法**：后台线程启动回补扫描 |
| L121-199 | **新增 `_retroactive_scan()` 方法**：全量建 import map + 遍历所有 .py 文件运行 `_pre_review_content()` + 汇总报告注入 Coda + 严重问题沉淀 |
| L201-221 | **`_init_import_map()` 重写**：使用 `get_review_mode()` 判断，off 模式跳过 |
| L227-302 | **`on_before_write()` 重写**：入口处调用 `_check_transition()` 检测模式切换；使用 `get_review_mode()` 实时读取；off 模式直接放行（return None） |
| L303-344 | **`on_file_written()` 重写**：import map 增量更新放入后台线程（Thread + daemon）；使用 `get_review_mode()` 判断 strict 模式；新增 `_async_update_import_map()` 后台执行 |
| L345-354 | **新增 `_async_update_import_map()` 方法**：后台线程执行增量更新，不阻塞主循环 |
| L356-386 | **`_spawn_review_sub_agent()`** 基本保持，微调日志格式 |
| L388-430 | **`_spawn_test_sub_agent()` 重写**：从 `git worktree` + `git checkout` 方案改为 **Docker 沙箱方案**。prompt 简化为调用 `sandbox_test` 工具，检测 `[SANDBOX_RESULT: FAIL]` |
| L436-462 | **`on_turn_end()` 重写**：入口处调用 `_check_transition()` 检测模式切换；等待 import map 后台线程完成（`join(timeout=5)`）后再检查 `_coda_findings` |
| L574-610 | **`_record_false_positive()` 和 `_is_false_positive()` 重写**：优先查 `_fp_cache` 内存缓存（避免每次读盘），加 `_fp_cache_lock` 保护并发 |

### 8. `review/memory.py` — **无修改**

审查发现持久化。ReviewFinding dataclass + ReviewMemoryStore CRUD。已有，本次未改。

### 9. `review/mode_engine.py` — **无修改**

四层严格触发判断（安全路径 / diff 特征 / 历史问题率 / 新人代码）。已有，本次未改。

### 10. `review/promotion.py` — **无修改**

审查发现→skill/全局记忆沉淀。已有，本次未改。

---

## 三、工具系统（新建 + 修改）

### 11. `tools/import_map.py` — **新建**

`minicode/tools/import_map.py`，约 100 行。纯 Python AST 符号索引。

| 函数 | 行号 | 功能 |
|:----|:----:|:-----|
| `_extract_symbols()` | L30-65 | AST 解析源代码提取顶层符号（函数/类/常量），判断公开/私有 |
| `_find_references()` | L80-100 | 单词边界正则搜索引用，跳过私有符号 |
| `build_import_map()` | L105-130 | 全量建表：遍历 .py → AST 提取符号 → 搜索引用 → 写 JSON，后台线程调用 |
| `update_import_map_for_file()` | L135-180 | 增量更新：删除旧符号 → 提取新符号 → 更新引用 → 写文件 |
| `_save_import_map()` | L195-210 | 原子写入（tmp → rename），防止写中断文件损坏 |
| `get_affected_files()` | L215-230 | O(1) 查表：直接读 JSON 返回受影响文件列表 |

### 12. `tools/sandbox_test.py` — **新建**

`minicode/tools/sandbox_test.py`，约 180 行。Docker 容器沙箱测试执行器。

| 类/函数 | 行号 | 功能 |
|:--------|:----:|:-----|
| `DockerSandbox` 类 | L30 | Docker 沙箱执行器 |
| `DockerSandbox.run()` | L44-65 | 完整流程：检查 Docker → 创建容器 → 安装依赖 → 拷贝文件 → 跑测试 → 清理 → 返回 PASS/FAIL |
| `DockerSandbox._check_docker()` | L68-77 | 检查 `docker info` 可用性 |
| `DockerSandbox._create_container()` | L79-112 | `docker run -d --rm python:3.11-slim`，只读挂载项目目录，自动处理 Windows 路径转换 |
| `DockerSandbox._install_deps()` | L114-121 | 容器内 `pip install -e /workspace` + `pip install pytest` |
| `DockerSandbox._run_tests()` | L130-160 | `pytest -x --tb=short -q` 300 秒超时，自动推断测试路径 |
| `DockerSandbox._cleanup()` | L162-170 | `docker rm -f` 销毁容器 |
| `_run()` 工具入口 | L200-230 | 返回 `[SANDBOX_RESULT: PASS]` 或 `[SANDBOX_RESULT: FAIL]` 结构化输出 |

### 13. `tools/task.py` — **修改**

| 行号 | 修改 |
|:----:|:-----|
| L63-82 | **新增 `review` Agent 类型**：只读工具集（read_file/grep_files/find_references/code_review/...），max_turns=8，prompt 要求输出结构化报告 |
| L83-98 | **新增 `test` Agent 类型**：工具集使用 `sandbox_test`（Docker 沙箱）+ 只读工具，max_turns=5，prompt 简化为调用 sandbox_test 工具 |
| L96 | `test` 的 `allowed_tools` 包含 `{"sandbox_test", "read_file", "grep_files", "list_files"}` |
| L301 | **`input_schema` 的 enum 扩展**：从 `["explore", "plan", "general"]` 改为 `["explore", "plan", "general", "review", "test"]`，description 同步更新 |

### 14. `tools/code_review.py` — **修改**

| 行号 | 修改 |
|:----:|:-----|
| L336-337 | **末尾追加 1 行**：`from minicode.review.hooks import _pre_review_content` — re-export，不重复实现。`_pre_review_content` 的唯一实现在 `hooks.py`，避免两份规则不同步 |

### 15. `tools/__init__.py` — **修改**

| 行号 | 修改 |
|:----:|:-----|
| L28 | **新增导入**：`from minicode.tools.sandbox_test import sandbox_test_tool` |
| L72 | **注册工具**：将 `sandbox_test_tool` 加入 `_CORE_TOOLS` 列表 |

---

## 四、导入路径迁移（agent_loop_lite → agent_loop）

`agent_loop_lite.py` 删除后，4 个外部引用文件统一修改导入路径：

### 16. `headless.py`

| 行号 | 修改 |
|:----:|:-----|
| L126 | `from minicode.agent_loop_lite import run_agent_turn` → `from minicode.agent_loop import run_agent_turn` |

### 17. `main.py`

| 行号 | 修改 |
|:----:|:-----|
| L22 | `from minicode.agent_loop_lite import run_agent_turn` → `from minicode.agent_loop import run_agent_turn` |

### 18. `tui/input_handler.py`

| 行号 | 修改 |
|:----:|:-----|
| L16 | `from minicode.agent_loop_lite import run_agent_turn` → `from minicode.agent_loop import run_agent_turn` |

### 19. `tools/task.py`

| 行号 | 修改 |
|:----:|:-----|
| L16 | `from minicode.agent_loop_lite import run_agent_turn` → `from minicode.agent_loop import run_agent_turn` |

---

## 五、文档

### 20. `docs/最终架构框架图.md` — **新建**

完整的项目全景图，包含：文件结构全景、core loop 三阶段图解、3 个钩子详细流程图、2 条子 Agent 链路、import map 机制、三级模式对比表、运行时模式切换说明（off→strict 回补扫描）、文件改动总账、面试话术。

### 21. `docs/项目全方位总结.md` — **新建**

全方位项目总结，重点覆盖：审查 agent 与 test agent、`run_agent_turn` 框架流程、session 系统、memory 系统、main 入口等核心文件梳理。

---

## 六、Bug 修复（预存语法错误）

以下 7 个文件在导入链路上有被注释破坏的语法错误（# 注释了控制流首行但未注释缩进块），已修复：

| 文件 | 错误位置 | 修复内容 |
|:-----|:--------|:---------|
| `logging_config.py` | 多处 | 恢复 `get_logger()` 函数体、`get_log_stats()` 变量赋值 |
| `intent_parser.py` | 多处 | 恢复 4 处 `# return`/`# for`/`# if` |
| `layered_context.py` | 多处 | 恢复 7 处 `# self.* =` 赋值 |
| `micro_compact.py` | 多处 | 恢复 2 处 `# if`/变量赋值 |
| `tools/__init__.py` | L85 | 恢复 `configured = (...)` 赋值 |
| `manage_cli.py` | 多处 | 恢复 `# print(` 函数调用 |
| `mcp.py` | 多处 | 恢复 2 处 `# for`/`# if` |

另有 19 个文件的同类问题通过批量脚本一并修复。修复后 **全部 102 个 .py 文件零语法错误**。

---

## 改动统计

| 类型 | 数量 | 净代码行数 |
|:-----|:----:|:----------:|
| **新文件** | 5 | ~380 行 |
| 新建：`model_caller.py` | | 280 行（从 agent_loop_lite.py 拆分） |
| 新建：`loop_orchestrator.py` | | 878 行（从 agent_loop_lite.py 拆分） |
| 新建：`tool_executor.py` | | 159 行（从 agent_loop_lite.py 拆分） |
| 新建：`tools/import_map.py` | | ~100 行 |
| 新建：`tools/sandbox_test.py` | | ~180 行 |
| **修改文件** | 10 | ~60 行 |
| 重写：`agent_loop.py` | | +11 行 |
| 改：`review/config.py` | | +7 行 |
| 改：`review/hooks.py` | | ~+200 行 |
| 改：`tools/task.py` | | +40 行 |
| 改：`tools/code_review.py` | | +1 行 |
| 改：`tools/__init__.py` | | +2 行 |
| 改：`headless.py` | | 1 字符 |
| 改：`main.py` | | 1 字符 |
| 改：`tui/input_handler.py` | | 1 字符 |
| 改：`loop_orchestrator.py` | | ~+20 行 |
| **删除文件** | 1 | -1225 行 |
| 删除：`agent_loop_lite.py` | | -1225 行 |
| **语法修复** | 7 文件 + 19 批量 | - |
| **文档** | 2 | ~+1000 行 |
| **总计** | | ~+400 行净代码 |
